/* interface Person {
  name: string;
  //물음표가 들어갔다는 것은, 설정을 해도 되고 안해도 되는 값
  age?: number;
}
interface Developer {
  name: string;
  age?: number;
  skills: string[];
}

const person: Person = {
  name: "강은빈",
  age: 18,
};

const expert: Developer = {
  name: "silverbeen",
  skills: ["react", "JS"],
};

const people : Person[] = [person, expert]; */