"use strict";
/* interface Point{
    readonly x: number;
    readonly y : number;
}

let p1: Point = {x:10, y :20};

let a: number[] = [1,2,3,4];
let ro: ReadonlyArray<number> = a;
ro[0] = 12; //에러
ro.push(5); //에러
ro.length = 100; //에러
a = ro; // 에러

a = ro as number[]; //가능
 */ 
